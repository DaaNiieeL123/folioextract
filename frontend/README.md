# FolioExtract Frontend

React/Vite interface for FolioExtract.

## Commands

```bash
npm install
npm run dev
npm run build
```

The frontend API base URL is controlled with `VITE_API_URL` during development.
